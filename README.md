# FireSmoke_YOLO11

YOLO11n-based fire and smoke video detection pipeline with adaptive keyframe selection. Advanced Algorithms course project.

## Project Goal

Compare three video inference strategies using YOLO11n as the frame scorer:

1. **Full-frame baseline** — YOLO on every frame
2. **Fixed-interval baseline** — YOLO on every k-th frame (k=10)
3. **Adaptive DP keyframe selection** — score-responsive variable-rate sampling with windowed dynamic programming keyframe selection

The adaptive method reduces the number of processed frames and inference runtime while preserving fire/smoke event coverage.

## Environment

```bash
conda activate yolo
```

- Python 3.10.20 · PyTorch 2.10.0 · Ultralytics 8.4.22 · MPS (Apple Silicon)
- Training was run on Google Colab T4 GPU.

## D-Fire Class Mapping

```
0 = smoke
1 = fire
```

`data.yaml` uses `names: ['smoke', 'fire']`. Inference scripts set `model.model.names` and `result.names` per inference call to ensure annotated outputs display the corrected labels (the model's original training metadata had these swapped).

## Scripts

| Script | Purpose |
|---|---|
| `01_check_dataset.py` | Validate raw D-Fire dataset structure and labels |
| `02_prepare_dfire_dataset.py` | Build clean train/valid/test splits from raw D-Fire |
| `03_train_yolo11n.py` | Fine-tune YOLO11n on the processed dataset |
| `04_validate_model.py` | Evaluate trained model on the test split |
| `05_full_frame_video.py` | Full-frame inference baseline |
| `06_fixed_interval_video.py` | Fixed-interval sampling baseline (k=10) |
| `07_adaptive_keyframe_video.py` | Adaptive DP keyframe selection |
| `08_evaluate_methods.py` | Generate per-video comparison CSVs and Markdown reports |
| `09_export_model.py` | Export model to ONNX/TFLite (stub) |
| `10_run_video_experiments.py` | Batch runner: all 3 methods across all input videos |
| `11_generate_result_figures.py` | Generate bar chart comparison figures from CSVs |

## Trained Model

YOLO11n fine-tuned on D-Fire (21,527 images, 2 classes) on Colab T4 GPU:

- 50 epochs · imgsz 640 · batch 16
- mAP50: ~0.754 · mAP50-95: ~0.442
- Checkpoint: `models/yolo11n_dfire_best.pt`

## Video Experiments

7 test videos evaluated at conf=0.60 on Apple MPS. Key results (frames processed / wall-clock runtime):

| Video | Full-frame | Fixed (k=10) | Adaptive DP | Detected |
|---|---|---|---|---|
| fire_smoke_test (597f) | 597f / 8.1s | 60f / 1.8s | 116f / 13.7s | All ✓ |
| smoke_only_test (587f) | 587f / 8.2s | 59f / 2.5s | 118f / 31.0s | All ✓ |
| normal_background (362f) | 362f / 3.8s | 37f / 1.0s | 14f / 1.2s | All ✗ |
| distant_smoke_fire (928f) | 928f / 9.3s | 93f / 1.8s | **58f / 1.5s** | All ✓ |
| dynamic_wildfire_drone (1237f) | 1232f / 12.0s | 124f / 2.1s | **97f / 1.8s** | All ✓ |
| hard_neg sunset/fog (476f) | 476f / 5.9s | 48f / 2.1s | 18f / 3.7s | All ✗ |
| hard_neg clouds/fog (2400f) | 2400f / 32.2s | 240f / 8.4s | 301f / ~10s | All FP |

✓ = event correctly detected · ✗ = correctly rejected · FP = false positive (model limitation: cloud/fog textures activate the smoke class)

Adaptive beats fixed-interval on both frame count and runtime for `distant_smoke_fire` and `dynamic_wildfire_drone`. On `hard_negative_sunset_fog`, adaptive processes only 18 of 476 frames (96% reduction).

Full per-video details: `reports/algorithm_explanation.md` and `reports/*_method_comparison.md`.

## Figures

Generated by `python scripts/11_generate_result_figures.py`:

```
results/figures/processed_frames_comparison.png
results/figures/runtime_comparison.png
results/figures/frame_reduction_comparison.png
```

## Repository Structure

```text
data/           raw + processed D-Fire images and labels
configs/        training configuration YAML
scripts/        numbered pipeline scripts (01–11)
notebooks/      Colab training notebook
models/         trained model weights
videos/input/   7 test MP4s
results/        frame scores, comparison tables, figures, keyframes
reports/        experiment log, algorithm explanation, per-video comparison reports
archive/        reserved for superseded outputs
```
